# STATUS — 프로젝트 현황 스냅샷

> 최종 갱신: 2026-07-08 · 브랜치 `main` · MVP 스캐폴드 완성, `MOCK_MODE=1` 데모 가능.

## 한 줄 요약

7개 영역 전부 구현 완료, `tsc` 0 에러 / `next build` 성공(32페이지) / mock E2E 스모크 통과.
4관점 감사에서 18건 발견 → critical/major 8건 수정. 실연동은 코드 경로만 존재(실키 미검증).

---

## 빌드 완료 영역

| 영역 | 상태 | 핵심 |
|---|---|---|
| **DB 스키마 + RLS + 크레딧 SQL** | ✅ | 테이블 6종, security definer 함수 8종, RLS 격리, 금전 테이블 직접 쓰기 차단(함수 경유만) |
| **데이터 계층 (mock/Supabase 이중)** | ✅ | `getDataServices()` 팩토리, 화로담·민트세탁소 시드, RPC 금전 처리 |
| **API 라우트** | ✅ | 인증·온보딩·사이트·크레딧·편집·결제(웹훅)·도메인·관리자·크론 |
| **캔버스 에디터** | ✅ | 드래그·8핸들 리사이즈·스냅·undo/redo·인스펙터·AI패널·자동저장·발행 |
| **멀티테넌트 렌더러** | ✅ | `proxy.ts` 호스트 라우팅, cqw 비례 스케일 SSR, 모바일 자동 스택 |
| **고객 대시보드 + 온보딩** | ✅ | 랜딩·로그인·위저드(설문→후보3안→생성)·사이트 상세·크레딧·결제·설정 |
| **관리자 콘솔** | ✅ | 오버뷰·고객관리·QA큐(승인/반려+환불)·인프라 모니터 |

## 검증됨 (직접 실행)

- `npx tsc --noEmit` → 0 에러
- `npm run build` → 성공 (32 페이지)
- mock 스모크: 랜딩/로그인 200, mock 로그인(premium 잔액 6), 사이트 목록(화로담 live),
  테넌트 서빙(`/s/hwarodam.anakslabs.com` 경로·Host헤더 모두 200), Basic 영상 402 업셀,
  대시보드/에디터/온보딩/관리자 SSR 200, 비관리자 `/admin` 차단(307)
- 온보딩 E2E: 설문 → 후보 3안(3D 렌더 포함) → 생성 → 발행(서브도메인 자동 할당) → 서빙

## 감사 결과 (18건 발견 → critical/major 8건 수정)

**수정 완료:**
- 🔴 결제 웹훅 무인증 크레딧 발급 취약점 → mock 게이트 + 서버 가격표 대조 + 토스 결제조회 재검증
- 🟠 QA 반려 순서(환불 우선으로 뒤집어 환불 실패 시 재시도 가능)
- 🟠 SQL 만료 FIFO 소급 귀속 버그 → 원장 시간순 리플레이로 재작성
- 🟠 href/미디어 src 저장형 XSS → 스킴 화이트리스트(`lib/safe-url.ts`) 저장·렌더 양쪽 적용
- 🟠 NumberField 입력 하이재킹(클램프를 blur/Enter로 지연)
- 🟠 **중첩 `<a>` 하이드레이션 에러** (대시보드 사이트 카드 Link 안의 렌더러 버튼 `<a>`)
  → `SiteRenderer`에 `interactive` 플래그 추가, 미리보기(`SitePreview`)는 버튼을 비대화형 `<span>`으로 렌더

## 남은 작업

**실연동** (→ [SETUP.md](SETUP.md)):
- [ ] Supabase 로컬/클라우드 실키 스모크 ⭐ **다음 단계로 선택됨**
- [ ] 카카오/구글 OAuth 앱 등록 + 실 로그인 검증
- [ ] Gemini/GLM 실키로 생성 품질 확인
- [ ] Veo 3.1 실구현 (현재 스텁 → 502+자동환불로 안전)
- [ ] 토스 결제창 SDK 클라이언트 연동 (사업자등록 선행)
- [ ] Cloudflare 존 + 도메인 소유 + Vercel 배포

**품질/후속:**
- [ ] 에디터 포인터 인터랙션 브라우저 수동 QA (드래그/리사이즈/스냅/인라인 편집)
- [ ] `supabase/seed.sql`의 화로담 config를 mock 품질(`HWARODAM_SITE_CONFIG`)로 동기화
- [ ] 감사 minor 잔여(회전 요소 리사이즈 축 보정, 에디터↔렌더러 오버플로우 WYSIWYG, ColorField 검증 등)
- [ ] 에디터 모바일 프리뷰(CanvasStage)도 비대화형 전환 검토 (현재 앵커 미중첩이라 에러는 없음)

## 알려진 제약

- mock 시드의 데모 계정 2명은 소셜 로그인 불가한 데이터 전용 계정 (실연동 시 신규 가입 플로우)
- `MOCK_MODE=0` 로컬에서는 로그인 페이지의 데모 버튼이 사라짐 (단계 2 OAuth 필요)
- 관리자 크레딧 조정 memo, QA 반려 사유는 현재 원장에 미기록 (계약 확장 필요)
- Next.js 16 — `middleware.ts` 금지(`proxy.ts` 사용). 두 파일 공존 시 빌드 에러
